#!/usr/bin/env pypy3
import sys
import random
import signal

signal.signal(signal.SIGPIPE, signal.SIG_DFL)

columns = int(sys.argv[1])

with open('/usr/share/dict/words') as f:
    words = f.read().splitlines()

while True:
    print(','.join(random.choice(words) for _ in range(columns)))
