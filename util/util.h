#ifndef UTIL_H
#define UTIL_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MIN(x, y) ((x < y) ? x : y)
#define BUFFER_SIZE 1024 * 1024 * 5
#define MAX_COLUMNS 65535
#define DELIMITER ','

#endif
